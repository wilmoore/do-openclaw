# OpenClaw

Secure self-hosted AI agent deployment on DigitalOcean behind Tailscale.

**Quick start:** `make deploy` (see `doc/getting-started.md` for prerequisites)

For full documentation, see the `doc/` directory.